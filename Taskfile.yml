version: '3'

vars:
  BINARY: weka-log-collector
  LINUX_BINARY: weka-log-collector
  LINUX_ARM64_BINARY: weka-log-collector-arm64

tasks:
  fmt:
    desc: Format code
    cmds:
      - gofmt -w .

  vet:
    desc: Static analysis
    cmds:
      - go vet ./...

  lint:
    desc: Run staticcheck linter
    cmds:
      - ~/go/bin/staticcheck ./...

  test:
    desc: Run unit tests
    cmds:
      - go test ./... -v

  build:
    desc: Build binary for current platform
    cmds:
      - go build -o {{.BINARY}} ./...

  build-linux:
    desc: Build portable static Linux binary for amd64 (for deployment to Weka nodes)
    cmds:
      - CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o {{.LINUX_BINARY}} ./...

  build-linux-arm64:
    desc: Build portable static Linux binary for arm64 (aarch64 nodes)
    cmds:
      - CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o {{.LINUX_ARM64_BINARY}} ./...

  check:
    desc: Run all quality checks (fmt, vet, lint, test, build, build-linux, build-linux-arm64)
    cmds:
      - task: fmt
      - task: vet
      - task: lint
      - task: test
      - task: build
      - task: build-linux
      - task: build-linux-arm64

  install-hooks:
    desc: Install git hooks (builds Linux binary and stages it on every commit)
    cmds:
      - cp hooks/pre-commit .git/hooks/pre-commit
      - chmod +x .git/hooks/pre-commit
      - echo "Git hooks installed."

  clean:
    desc: Remove built binaries
    cmds:
      - rm -f {{.BINARY}} {{.LINUX_BINARY}} {{.LINUX_ARM64_BINARY}}
